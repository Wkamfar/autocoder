node_modules/
dist/
.env
state/
*.log
.DS_Store

# Brain is a separate git repo (knowledge layer); runtime + private remotes live here
brain/

# Local-only Claude Code permissions (may reference machine paths / hosts)
.claude/settings.local.json
